<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ljq.bishe.mapper.WorkScoreMapper">
  <select id="findStudentScore">
    select s.*,h.filepath from score s,selectcourse sc,homework h
   where sc.stuid=s.stuid and sc.courseclass=#{course} and coursename=#{courseClass} and s.state=#{state} and s.workid=h.workid;
  </select>
</mapper>